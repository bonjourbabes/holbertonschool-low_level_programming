#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

/**
 * print_list - check the code
 * @h: variable
 * Return: Always 0.
 */
size_t print_list(const list_t *h)
{
	list_t *n = (list_t *)h;
	int i = 0;
	
	if (n == NULL)
	{
		printf("[0] (nil)\n");
	}
	while (h != NULL)
	{
		if ()
	}

	
}