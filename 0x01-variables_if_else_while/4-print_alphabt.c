#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
*main - uses putchar
*Return: return value 0
*/
int main(void)
{
char c;

for (c = 'a' ; c <= 'z' ; c++)
if (c =!'e') && (c =! 'q')
putchar(c);
putchar('\n');
return (0);
}
