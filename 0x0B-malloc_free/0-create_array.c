#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * create_array - creates array of chars and initlaize
 * @size: the address of memory to print
 * @c: the size of the memory to print
 * Return: Nothing.
 */
char *create_array(unsigned int size, char c)
{
	int i = 0;
	int *arr = malloc(sizeof(char))
	if (size == NULL)
	{
		return (0);
	}
	while (i < size)
	{

	}
}
